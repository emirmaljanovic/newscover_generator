import React from 'react';

import './header.css';

const Header = () => (
  <header className="main-header">
    <div className="add">
      <span>5% OFF everything coupon</span>
    </div>
    <h1>Recap Weekly</h1>
  </header>
);

export default Header;